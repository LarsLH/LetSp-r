{
  "type": "object",
  "properties": {
    "sheetMode": {
      "type": "string",
      "enum": ["daily", "exam","special"],
      "description": "Vælg tilstand for blokerede websteder (daglig, eksamen eller special)."
    },
    "dailyCsvUrl": {
      "type": "string",
      "description": "URL til CSV-fil for daglig blokering."
    },
    "examCsvUrl": {
      "type": "string",
      "description": "URL til CSV-fil for eksamensblokering."
    },
    "specialCsvUrl": {
      "type": "string",
      "description": "URL til CSV-fil for specialblokering."
    },    
    "blockedPageMode": {
      "type": "string",
      "enum": ["internal", "external"],
      "description": "Vælg tilstand for blokeret side (indbygget eller ekstern)."
    },
    "blockedPageUrl": {
      "type": "string",
      "description": "URL til ekstern blokeret side."
    },
    "updateInterval": {
      "type": "integer",
      "description": "Opdateringsinterval i millisekunder."
  }
 }
}